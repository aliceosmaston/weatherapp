//Interface used as an observable for fetching data from the fake API 
  
    export interface City {
     name: string;
     username: string;
     email: string;


//Weather data to be returned on each city from openweathermap API
/*
    id: number;
    name: string;
    lon: number;
    lat: number;
    temp: number;
    pressure: number;
    humidity: number;
    temp_min: number;
    temp_max: number;
    dt: number;
    speed: number;
    deg: number;
    all: string;
    id: number;
    main: string;
    description:
    icon: string;
*/
    
}
